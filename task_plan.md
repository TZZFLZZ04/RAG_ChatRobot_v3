# 任务计划：分步审查与重构 ChatRobot

## 目标
依据项目分层约束，完成 RAG 递归切分的只读审查，并交付可供后续新对话实施的最小、可验证修改方案；本对话不修改业务代码。

## 当前阶段
阶段 14

## 各阶段

### 阶段 1：访谈与范围确认
- [x] 读取指定技能与项目参考文档
- [x] 确认首个审查范围为 RAG 检索链路
- [x] 确认候选改进：递归切分、语义切分、混合检索、上下文感知检索
- [x] 确认上下文感知检索发生在索引阶段：LLM 为分块生成上下文前缀
- [x] 确认第一批仅尝试递归切分，语义切分延期
- [x] 将当前确认结果记录到 findings.md
- **状态：** complete

### 阶段 2：基线审查
- [x] 确认当前递归切分实现与调用路径
- [x] 检查依赖版本、配置文档和相关测试覆盖
- [x] 记录具体问题、证据、风险和候选最小改动
- [x] 运行相关现有测试建立基线
- **状态：** complete

### 阶段 3：编写修改方案
- [x] 明确建议修改的文件、接口和配置
- [x] 给出分步实现顺序与每步验收标准
- [x] 明确不应修改的范围与兼容性边界
- **状态：** complete

### 阶段 4：方案验证与复审
- [x] 检查方案能否覆盖中文、英文、元数据和边界输入
- [x] 列出后续实现对话应运行的测试命令
- [x] 复查每项建议是否均可追溯到递归切分目标
- **状态：** complete

### 阶段 5：交付
- [x] 汇总基线、建议、验收标准与风险
- [x] 标注语义切分、混合检索、上下文感知检索为后续范围
- [x] 交付给后续代码修改对话
- **状态：** complete

## 关键问题
1. 首个审查与重构范围是什么？（已确认：RAG 检索链路）
2. 该范围内最希望优先改善的质量属性是什么？（已确认：RAG 回答正确率）
3. “上下文感知检索”具体指什么？（已确认：索引前生成块级上下文前缀，再以“前缀 + 原块”建立索引）
4. 递归切分和语义切分应按什么顺序或配置方式组合？（当前决定：第一批仅递归切分，语义切分延期）
5. 哪些现有行为、接口或兼容性绝不能改变？
6. 用什么数据集和指标验证正确率提升？

## 已做决策
| 决策 | 理由 |
|------|------|
| 先访谈、后审查、再逐项实施 | 用户要求使用 grill-me 并一步步更新 |
| 每批只处理一个明确范围 | 降低无关改动和回归风险 |
| 行为改变必须有测试证据 | 使验收标准可验证 |
| 首批仅聚焦 RAG 检索链路 | 用户明确指定该范围 |
| 允许审查与切分直接相关的入库链路 | 递归切分与语义切分必须在索引前执行 |
| 术语统一为“上下文感知检索” | 用户纠正术语并提供了明确流程图 |
| 上下文化在索引阶段执行 | 图示明确由 LLM 为分块生成前缀，再拼接原块建立索引 |
| 第一批只处理递归切分 | 用户要求先尝试递归切分，避免同时引入语义切分复杂度 |
| 不重复引入已有的递归切分类 | 基线代码已经使用 `RecursiveCharacterTextSplitter`，应聚焦可验证缺口 |

## 遇到的错误
| 错误 | 尝试次数 | 解决方案 |
|------|---------|---------|
| 首次合并读取输出发生截断和乱码 | 1 | 改用 UTF-8 分段读取，已完整读取 |
| 当前目录执行 git status 报告不是 Git 仓库 | 1 | 记录环境事实，待确认仓库来源后再使用 Git/GitHub 上下文 |
| 规划技能 `check-complete.ps1` 因乱码产生 PowerShell 解析错误 | 1 | 不重复运行损坏脚本，改用文本检查阶段状态 |
| PDF 包装器路径无法执行且不存在 `pdftotext.cmd` | 1 | 改用 bundled Poppler 原生 exe 与 bundled Python pdfplumber |
| PowerShell `python -c` 转义破坏页码字典键 | 1 | 改用 apply_patch 创建临时 Python 检查脚本 |
| `rg` 健康检查正则因 PowerShell 引号被截断 | 1 | 改用单引号固定字符串分别搜索，不重复原命令 |

## 备注
- 重大决策前重新读取本文件。
- 上一轮仅允许修改规划文档；本轮用户已明确授权按 `findings.md` 方案实施递归切分。
- 后续实现只修改用户确认范围；不顺手清理无关代码。

## 阶段 6：递归切分实施与验证（2026-08-10）
- [x] 新增中英文句界、无分隔符兜底、映射兼容性测试，并确认旧实现不满足句界预期
- [x] 仅完善 `app/rag/splitters.py` 的递归分隔符与 `keep_separator` 配置
- [x] 更新 README 中的字符计量、重叠约束和重建索引说明
- [x] 运行切分器、入库路径、检索/评估及全量测试
- **状态：** complete

## 阶段 7：指定 PDF 评测资产（2026-08-10）
- [x] 检查 PDF 元数据、页数、可提取文本和版面
- [x] 从原文生成可追溯的问题、参考答案与预期关键词
- [x] 创建兼容现有 `RagEvaluationDataset` 的 JSON 数据集
- [x] 创建后续基线、重建、复测与对比操作说明
- [x] 校验 JSON schema、案例唯一性、来源名称和证据页码
- **状态：** complete

## 阶段 8：评估页知识库选择优化（2026-08-11）
- [x] 补充页面与脚本契约测试，复现当前依赖手填 `collection_id` 的缺口
- [x] 从 `/api/v1/collections` 加载当前用户知识库并渲染可访问的选择控件
- [x] 选择知识库后以对应 `collection.id` 运行评估，并完善加载、空列表与运行中状态
- [x] 运行前端页面、评估 API 与全量回归测试
- [x] 在本地页面验证桌面与窄屏布局和交互
- **状态：** complete

## 阶段 9：后台评估、进度查询与报告排序（2026-08-11）
- [x] 新增失败测试：报告按 `created_at` 倒序、评估提交返回后台任务、进度状态可查询
- [x] 为评估 service 增加逐 case 进度回调，并由 Celery 任务发布进度
- [x] 将评估运行 API 改为 202 异步提交，增加任务状态查询端点
- [x] 前端增加“已完成 X/总数”进度条、轮询、完成与失败反馈
- [x] 更新 Worker/README 运行说明并完成针对性、全量及浏览器验证
- **状态：** complete

## 阶段 10：更新 AI 上下文文档（2026-08-11）
- [x] 读取现有规划文件、仓库文件树、README、配置、RAG 切分、评估 API、Worker、任务队列、schema、前端页面和测试入口
- [x] 更新 `AGENTS.md` 为最新仓库协作指南
- [x] 更新 `AGENTS_a_c.md` 为最新 AI 架构上下文
- [x] 运行文档相关的静态/契约测试并记录结果
- **状态：** complete

## 阶段 11：RAG 评测改进与 LangSmith 实施流程（2026-08-11）
- [x] 恢复现有规划、发现、进度和最新架构上下文
- [x] 汇总检索标签、回答完整性、Judge 独立性和延迟问题
- [x] 明确 LangSmith 为可选增强层，不替代本地真值和评估看板
- [x] 编写分阶段实施、数据模型、指标、测试、风险和验收标准
- [x] 创建 `proces.md` 并核对其结构完整性
- **状态：** complete

## 阶段 12：再次刷新 AI 上下文文档（2026-08-12）
- [x] 确认仓库实际文件名为 `AGENTS.md` 与 `AGENTS_a_c.md`，不存在 `AGENT.md` / `AGNET_a_c.md`
- [x] 读取最新文件树、规划记录、`proces.md`、`docs/` 变更说明、前端源码契约和页面测试
- [x] 更新 `AGENTS.md` 和 `AGENTS_a_c.md`
- [x] 运行相关测试并记录结果
- **状态：** complete

## 阶段 13：Google Cloud 部署就绪审计（2026-08-17）
- [x] 盘点应用进程、Docker Compose、环境变量、数据库迁移与持久化路径
- [x] 核对 Google Cloud 目标架构选项及当前平台约束
- [x] 识别阻断上线的缺失配置、安全风险和可选生产增强
- [x] 给出最小可行部署步骤、推荐拓扑与验收清单
- **状态：** complete

## 阶段 14：生产容器配置第一步（2026-08-17）
- [ ] 通过 grill-me 确认生产 Compose 的后端、密钥来源与公网入口边界（已确认单机 GCE + FAISS + `.env.production`，待确认入口）
- [ ] 明确最小修改文件、兼容边界和验收标准并由用户确认
- [ ] 新增 `.dockerignore`、生产 Dockerfile、生产 Compose 与必要的环境变量模板
- [ ] 验证镜像上下文不含敏感/运行数据、Compose 可解析、容器可构建与相关测试通过
- [ ] 汇总本步骤配置方式、部署命令、风险和下一步
- **状态：** in_progress

## 阶段 15：proces.md 阶段 1A——测量诚实化（2026-08-20）
- [x] 先补充 D1、D6、429 重试与旧报告兼容的失败测试（基线 6 failed、20 passed）
- [x] 最小实现不可测/source-only 语义、检索项字段透传和可配置指数退避
- [x] 显式锁定 `langsmith==0.7.37`，并核对 `.env` / `.env.example` 的 tracing 环境变量均未置为真
- [x] 更新评估前端展示及页面契约，完成 1280px 与 375px 验收（无横向溢出、控制台无错误）
- [x] 运行定向测试、31 案例注入式重试验证与全量回归（27 passed；主机与容器全量均为 90 passed、1 skipped）
- [x] 验证现有 12 份报告全部可读取，三份 DeepSeek source-only 旧报告转换为 `primary_metrics=null`
- [x] 经用户明确授权后完成真实端到端运行，产出 31/31 答案与 31/31 Judge 的持久化报告
- [x] 汇总修改文件、测试结果、剩余风险和阶段 1B 下一步
- **状态：** complete

### 阶段 15 完成记录
- 真实报告：`data/evals/reports/20260820T035645910069Z-deepseek-academic-prompts-eval.json`，31/31 有答案、31/31 有 Judge、无最终失败。
- 报告主层级为 `source-only`、`primary_metrics=null`、`source_match=1.0`；155/155 个检索项均带 `page`、`content`、`metadata`。
- 真实运行本次未触发 429；31 案例注入测试覆盖答案与 Judge 各先返回 429 后全部成功，独立测试确认退避序列为 0.25/0.5/1.0 秒。
- Docker API/Worker 已重建并运行，容器内 `langsmith==0.7.37`；评估页及脚本返回 200，Worker 已连接 Redis 并注册评估任务。
- 修改文件：`app/schemas/rag_eval.py`、`app/core/config.py`、`app/services/rag_evaluation_service.py`、`app/frontend/evaluations.html`、`app/frontend/static/evaluations.js`、`.env.example`、`requirements.txt`、三份相关测试及本计划。
- 仓库 `venv` 仍指向已不存在的 Python 3.10.11，故本轮测试使用系统 Python 3.11.7；未擅自重建虚拟环境。
- 剩余风险：source-only 的 `source_match=1.0` 仍然饱和，不能证明段落召回质量；真实运行未实际遇到 429；除 langsmith 外的依赖仍大多未锁版本；Celery 容器仍有既有 root 用户告警。
- 阶段 1B 下一步：按 `proces.md` 实施 Milvus 句柄复用、FAISS 索引缓存及阶段计时，并用 no-diff 测试保证召回集合与排序不变。
