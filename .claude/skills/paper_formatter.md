---
name: paper-expert
description: 用于检查学术论文格式和 LaTeX 公式规范
---
当用户要求检查论文时，请执行以下步骤：
1. 检查所有的 LaTeX 公式是否使用了 $...$ 或 $$...$$ 包装。
2. 确保术语统一（例如：使用 RFAConv 而不是 C2f_RFA）。
3. 调用 `ls` 检查项目目录下的 images 文件夹是否存在引用。